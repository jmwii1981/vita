/*
    NOTE - Dynamic conjunctions, articles

    a vs an vowels: if a e i o u (an)

        "definite article" is 'the'
        "indefinite article" 'a' or 'an'

    singular vs plural (last letter is an 's')

    'and' only used to join x and y, or if last part of sentence
*/

const sentenceBuilderFactory = function( ) {
/* 
    Create p tag here, then append do it as you wish
*/
    // data = JSON.stringify(data);
    // data = JSON.parse(data);
}
// sentenceFactory('myWeatherEl', sentenceObject)

export { sentenceBuilderFactory };